pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "*.{js,ts,tsx}"
      run: eslint {staged_files}
    prettier:
      glob: "*.{js,ts,tsx,css,scss,md,json,yml}"
      run: prettier --write {staged_files}
    cspell:
      glob: "*"
      run: cspell {staged_files}
