pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,tsx,json}"
      run: pnpm dlx @biomejs/biome check {staged_files}
    cspell:
      glob: "*"
      exclude: "pnpm-lock.yaml"
      run: pnpm cspell {staged_files}
